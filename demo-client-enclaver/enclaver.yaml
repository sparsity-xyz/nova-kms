version: v1
name: "nova-kms-client-enclaver"
target: "nitro-enclave"

sources:
  - Dockerfile

ingress:
  - listen_port: 8000
  - listen_port: 18001

egress:
  allow:
    - 0.0.0.0/0:443
    - 0.0.0.0/0:80

env:
  - IN_ENCLAVE
  - TEST_CYCLE_INTERVAL_SECONDS
  - FIXED_DERIVE_PATH
  - KV_DATA_KEY
  - ODYN_ENDPOINT
  - ODYN_TIMEOUT_SECONDS
