version: v1
name: "nova-kms-client-capsule"
target: "nitro-enclave"

sources:
  app: "."

ingress:
  - listen_port: 8000
  - listen_port: 18001

egress:
  allow:
    - "0.0.0.0/0"
    - "::/0"
    - "**"
