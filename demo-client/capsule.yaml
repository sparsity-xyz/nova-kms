version: v1
name: "nova-kms-client"
target: "nitro-enclave"

sources:
  app: "."

ingress:
  - listen_port: 8000
  - listen_port: 18001

egress:
  allow:
    - "0.0.0.0/0"
    - "::/0"
    - "**"

env:
  - IN_ENCLAVE
  - HELIOS_RPC_URL
  - CAPSULE_ENDPOINT
  - CAPSULE_TIMEOUT_SECONDS
